apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    service: db
  name: db
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: {{ .Values.core.db.storage }}
