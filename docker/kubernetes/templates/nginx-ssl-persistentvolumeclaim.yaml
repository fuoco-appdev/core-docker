apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    service: nginx
  name: nginx-ssl-volume-claim
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: {{ .Values.nginx.sslStorage }}
