apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    service: db
  name: nginx-config-volume-claim
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: {{ .Values.nginx.configStorage }}
