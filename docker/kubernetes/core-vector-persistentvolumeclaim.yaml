apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    service: vector
  name: vector-volume-claim
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 100Mi
