events {
    worker_connections 1024;
}

http {
    map $http_upgrade $connection_upgrade {
        default upgrade;
        `` close;
    }

    ssl_password_file /etc/ssl/domain.pass;

    $NGINX_CORE_CONFIG
    $NGINX_ECOMMERCE_CONFIG
    $NGINX_AI_CONFIG
}